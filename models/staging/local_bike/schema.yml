version: 2

models:
  - name: stg_local_bike_brands
    description: "This model contains information about brands where products come from"
    columns:
      - name: brand_id
        description: "Primary key, unique identifier for each brand" #: generated by concatenating feedback_id and order_id.
        tests:
            - unique
            - not_null
      # - name: order_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id

  - name: stg_local_bike_categories
    description: "This model contains information about product categories"
    columns:
      - name: category_id
        description: "Primary key, unique identifier for each category" #: generated by concatenating feedback_id and order_id.
        tests:
            - unique
            - not_null 

  - name: stg_local_bike_customers
    description: "This model contains information about customers"
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each customer" #: generated by concatenating feedback_id and order_id.
        tests:
            - unique
            - not_null                 

  - name: stg_local_bike_order_items
    description: "This model contains information about order_items"
    columns:
      - name: order_items_id
        description: "Primary key, unique identifier for each customer: generated by concatenating order_id and item_id."
        tests:
            - unique
            - not_null 
      # - name: order_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id
      # - name: item_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id
      # - name: product_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id

  - name: stg_local_bike_order
    description: "This model contains information about orders"
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
            - unique
            - not_null 
      # - name: customer_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id
      # - name: store_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id
      # - name: staff_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id

                  
  - name: stg_local_bike_products
    description: "This model contains information about products"
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
            - unique
            - not_null 
      # - name: brand_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id
      # - name: category_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id


  - name: stg_local_bike_staffs
    description: "This model contains information about staffs"
    columns:
      - name: staff_id
        description: "Primary key, unique identifier for each staff."
        tests:
            - unique
            - not_null 
      # - name: store_id
      #   description: "Foreign key linking the feedback to the corresponding order."
      #   tests:
      #     - relationships:
      #         name: order_id_foreign_key_in_stg_sales_database__feedback
      #         to: ref('stg_local_bike_products')
      #         field: order_id


  - name: stg_local_bike_stocks
    description: "This model contains information about stocks"
    columns:
      - name: stock_id
        description: "Primary key, unique identifier for each staff. Generated by concatenating store_id and product_id" 
        tests:
            - unique
            - not_null  

  - name: stg_local_bike_stores
    description: "This model contains information about stores"
    columns:
      - name: store_id
        description: "Primary key, unique identifier for each store." 
        tests:
            - unique
            - not_null 